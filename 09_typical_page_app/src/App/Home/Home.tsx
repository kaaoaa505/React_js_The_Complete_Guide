import './Home.scss';

import Card from '../_partial/Card/Card';

const Home = (props: any) => {
  return (
    <Card className="Home">
      <h1>Welcome back!</h1>
    </Card>
  );
};

export default Home;
