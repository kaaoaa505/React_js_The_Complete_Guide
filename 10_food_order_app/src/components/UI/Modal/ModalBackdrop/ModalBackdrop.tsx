import './ModalBackdrop.scss';

const ModalBackdrop = (props: any) => {
    return (
        <div className='ModalBackdrop' onClick={props.onClick}>
        </div>
    )
}

export default ModalBackdrop;